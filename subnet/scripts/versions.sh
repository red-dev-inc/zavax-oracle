#!/usr/bin/env bash

# Don't export them as they're used in the context of other calls
avalanche_version=${AVALANCHE_VERSION:-'v1.13.0'}

avalanche_network_runner_version=${AVALANCHE_NETWORK_RUNNER_VERSION:-'v1.8.3'}
